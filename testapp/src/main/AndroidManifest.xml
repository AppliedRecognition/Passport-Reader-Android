<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appliedrec.mrtd_reader_sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".MRTDReaderApplication"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScanResultActivity" android:label="@string/travel_document" />

        <meta-data
            android:name="com.appliedrec.verid.password"
            android:value="${veridPassword}" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="${fabricApiKey}"
            />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>